#!/usr/bin/env python3
"""This module implements functions to compile images to test the ingredient
model. Ingredient model expects a grid made up of individual images."""


# import os

# from food_project.image_classification.crf.preprocess import GridImageCollector
# from food_project.image_classification.crf.test import (make_test_image,
#                                                         read_individual_image)

# test_raw_image_dir = "data/crf/test_images/raw/"
# compiled_image = make_test_image(n=3, k=4, test_image_dir=test_raw_image_dir)
# grid_image = GridImageCollector(compiled_image, 3, 3)

# for path in os.listdir(test_raw_image_dir):
#     image = read_individual_image(test_raw_image_dir + path)
